    import XCTest
    @testable import IFStory

    final class IFStoryTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(IFStory().text, "Hello, World!")
        }
    }
